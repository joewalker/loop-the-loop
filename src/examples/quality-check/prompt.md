Please review the `{{file}}` file.

Take care to understand the full context. Consider all the source files in `src/**` along with any other files needed to understand the full-context. Report on any bugs or serious issues that you find. Don't attempt to fix any problems.

Before reporting on any suspected issues, search on github issues to check that it's not a known issue.

Bug severity information is stored in [bug-tracking.md](/docs/bug-tracking.md). Use the S1-S4 rating system to categorize issues.

When you're sure about any issue, report it via the `gh` command. Include full details in the description including:

- Observed behavior
- Expected behavior
- Minimal reproduction
- Details of a fix if they're obvious

When you have finished raising issues with GitHub, update chat with a list of the issues found reporting on the severity, issues link and title.

Finally report on the tools that you would have liked to have used in this investigation, but that you didn't have access to.
